import java.util.PriorityQueue;

public class AStarSearch extends SearchAlgorithmAbstract {
    private PriorityQueue<StateNodeInterface> queue;

    @Override
    public boolean performSearch(StateNodeInterface initialState) {
        return false;
    }
}
